# Public Convex deployment URL, inlined into the client bundle at build time.
# NEXT_PUBLIC_* vars must be present during `next build` (the wrangler.jsonc
# runtime var does NOT reach the client bundle).
NEXT_PUBLIC_CONVEX_URL=https://astute-minnow-128.convex.cloud
